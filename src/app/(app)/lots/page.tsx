import { PlaceholderPage } from "@/components/placeholder-page";

export default function LotsPage() {
  return (
    <PlaceholderPage
      title="Lots"
      description="Search, direction filter, and status tabs over every lot."
      phase="Phase 4"
    />
  );
}
