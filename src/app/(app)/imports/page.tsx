import { PlaceholderPage } from "@/components/placeholder-page";

export default function ImportsPage() {
  return (
    <PlaceholderPage
      title="Imports"
      description="Inbound pipeline (origin → port → warehouse), grouped by status."
      phase="Phase 5"
    />
  );
}
