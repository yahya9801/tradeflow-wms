import { PlaceholderPage } from "@/components/placeholder-page";

export default function ExportsPage() {
  return (
    <PlaceholderPage
      title="Exports"
      description="Outbound pipeline (warehouse → destination), grouped by status."
      phase="Phase 5"
    />
  );
}
